{
  "$schema": "https://claude.ai/code/settings-schema.json",
  "permissions": {
    "allow": [
      "Bash(dotnet build*)",
      "Bash(dotnet clean*)",
      "Bash(dotnet restore*)",
      "WebFetch(domain:code.claude.com)"
    ]
  }
}
